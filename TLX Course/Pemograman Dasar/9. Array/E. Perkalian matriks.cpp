#include <iostream>

int main(){
    using namespace std;
    ios_base::sync_with_stdio(false);
    cout.tie(NULL);
    cin.tie(NULL);
	int n, m, p;
	int matriksA, matriksB;
	cin >> n >> m >> p;
	
	for(int i = 0; i < n; i++){
		for(int j = 0; j < m; j++){
			
		}
	}
}